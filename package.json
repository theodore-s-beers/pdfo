{
  "name": "pdfo",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "Theo Beers",
  "main": "pdfo.ts",
  "scripts": {
    "all": "dum format && dum lint && dum check && dum compile && dum simple",
    "check": "tsc",
    "compile": "esbuild sample.ts --bundle --minify --sourcemap --outfile=sample.min.js",
    "format": "prettier --write --plugin-search-dir=. .",
    "lint": "eslint --ignore-path .prettierignore --fix .",
    "simple": "esbuild pdfo.ts --legal-comments=none --minify --outfile=pdfo.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "@typescript-eslint/eslint-plugin": "^5.59.2",
    "@typescript-eslint/parser": "^5.59.2",
    "esbuild": "^0.17.18",
    "eslint": "^8.41.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4",
    "user-agent-data-types": "^0.3.1"
  }
}
