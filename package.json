{
  "name": "pdfo",
  "version": "0.1.0",
  "description": "TBD",
  "license": "MIT",
  "author": "Theo Beers",
  "type": "module",
  "main": "pdfo.ts",
  "scripts": {
    "all": "pnpm run lint && pnpm run check && pnpm run compile && pnpm run simple",
    "check": "tsc",
    "compile": "esbuild sample.ts --bundle --legal-comments=none --minify --sourcemap --outfile=sample.min.js",
    "format": "oxfmt",
    "knip": "knip",
    "lint": "oxfmt --check && oxlint",
    "simple": "esbuild pdfo.ts --legal-comments=none --minify --sourcemap --outfile=pdfo.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "knip": "^6.10.0",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "typescript": "^6.0.3",
    "user-agent-data-types": "^0.4.3"
  }
}
